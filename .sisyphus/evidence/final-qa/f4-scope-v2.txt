Tasks [7/7 compliant]:
  T1 action.yml: COMPLIANT — matches the plan acceptance criteria: 13 documented inputs, 3 outputs wired to steps.run/steps.post, and runs.using is composite; pinned install/chmod/env wiring align with the T1 spec.
  T2 prompts: COMPLIANT — prompts/code-review.md and prompts/security-review.md exist, are within the plan size bounds, and each contains a clear git diff instruction as required by T2; git command examples in prompt text are allowed by the plan.
  T3 prompts: COMPLIANT — prompts/dependency-review.md and prompts/test-coverage.md exist, are within the plan size bounds, and dependency-review.md mentions at least 3 dependency file patterns as required by T3.
  T4 post-comment.sh: COMPLIANT — uses --body-file, performs the sentinel reaction create/delete flow, exits 0 on missing REVIEW_FILE and permission-denied sentinel failure, and writes comment-url output exactly within T4 scope.
  T5 run-reviews.sh: COMPLIANT — keeps COPILOT_AUTO_UPDATE=false on both copilot invocations, passes --no-ask-user and --model in each invocation, quotes INPUT_ALLOWED_TOOLS in the copilot call, replaces final review output with fusion output when enabled, and exits 0 with warning when all reviews fail.
  T6 README.md: COMPLIANT — includes a prominent Prerequisites section before examples, documents all 13 inputs, mentions fetch-depth: 0 and fine-grained PATs, includes the pull_request_target warning, and provides at least 4 usage examples.
  T7 example workflow: COMPLIANT — uses pull_request (not pull_request_target), includes fetch-depth: 0, permissions.pull-requests: write, and secrets-based github-token/copilot-token wiring exactly as required.

Forbidden patterns [CLEAN]:
  pulls.*comments: CLEAN (no matches in scripts/ or action.yml)
  position|diff_hunk: CLEAN (no matches in scripts/ or action.yml)
  pull_request_target: CLEAN (no matches in .github/)
  @latest: CLEAN (no matches in action.yml or scripts/)
  suspicious 2>&1 in scripts/: CLEAN (no forbidden matches)

VERDICT: APPROVE
