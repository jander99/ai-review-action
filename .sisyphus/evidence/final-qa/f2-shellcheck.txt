=== F2 CODE QUALITY REVIEW — SHELLCHECK + ACTIONLINT ===
Date: 2026-04-27
Branch: feature/copilot-review-action
Commit: b30aa56

--- shellcheck scripts/run-reviews.sh ---
Command: docker run --rm -v "$(pwd):/mnt" koalaman/shellcheck:stable /mnt/scripts/run-reviews.sh
Exit code: 0
Output: (no findings)
RESULT: PASS

--- shellcheck scripts/post-comment.sh ---
Command: docker run --rm -v "$(pwd):/mnt" koalaman/shellcheck:stable /mnt/scripts/post-comment.sh
Exit code: 0
Output: (no findings)
RESULT: PASS

--- actionlint .github/workflows/example-copilot-review.yml ---
Command: docker run --rm -v "$(pwd):/repo" rhysd/actionlint:latest /repo/.github/workflows/example-copilot-review.yml
Exit code: 0
Output: (no findings)
RESULT: PASS

--- actionlint action.yml ---
Command: docker run --rm -v "$(pwd):/repo" rhysd/actionlint:latest /repo/action.yml
Exit code: 1
Output:
  repo/action.yml:1:1: "jobs" section is missing in workflow [syntax-check]
  repo/action.yml:1:1: "on" section is missing in workflow [syntax-check]
  repo/action.yml:3:1: unexpected key "description" for "workflow" section ...
  repo/action.yml:6:1: unexpected key "author" ...
  repo/action.yml:8:1: unexpected key "branding" ...
  repo/action.yml:12:1: unexpected key "inputs" ...
  repo/action.yml:91:1: unexpected key "outputs" ...
  repo/action.yml:107:1: unexpected key "runs" ...
NOTE: actionlint is designed ONLY for GitHub workflow files (which require `on:` and `jobs:`).
      action.yml is a composite action definition file — it uses `runs:`, `inputs:`, `outputs:`
      which are VALID keys for composite actions per the GitHub Actions schema.
      These errors are FALSE POSITIVES from tool misapplication. NOT real defects.
RESULT: FALSE POSITIVE (not counted as failure)

--- SCRIPT QUALITY CHECKS ---

run-reviews.sh:
  [PASS] #!/usr/bin/env bash on line 1
  [PASS] set -euo pipefail on line 2
  [PASS] mktemp used for all temp files (lines 119, 175, 209, 210)
  [PASS] trap cleanup EXIT on line 42 with tracked cleanup_files array
  [PASS] No eval anywhere in script
  [PASS] GITHUB_OUTPUT multi-line uses heredoc pattern (lines 244-248): echo "review<<EOF_REVIEW"
  [PASS] INPUT_ALLOWED_TOOLS is quoted: --allow-tool="${INPUT_ALLOWED_TOOLS:-shell(git:*)}"
  [PASS] No unused variables found
  [PASS] Comments add value (step numbers, env var documentation block at top)
  [INFO] Line 35: "${cleanup_files[@]:-}" intentional — guards against set -u error on empty array

post-comment.sh:
  [PASS] #!/usr/bin/env bash on line 1
  [PASS] set -euo pipefail on line 2
  [PASS] mktemp used for temp file (line 75)
  [PASS] trap 'rm -f "${tmp_body}"' EXIT on line 78
  [PASS] No eval anywhere in script
  [PASS] : "${VAR:?error}" pattern (lines 41-43) for required var validation
  [MINOR] Line 86: explicit rm -f "${tmp_body}" in error handler is redundant (trap will fire on exit 1)
           Not a correctness issue, just mild defensive duplication. NOT a reject-level finding.

--- AI ANTI-PATTERN CHECK ---

run-reviews.sh:
  [PASS] No TODO/FIXME/HACK comments
  [PASS] No redundant null checks
  [PASS] No dead code paths
  [PASS] Error messages are concise and non-repetitive

post-comment.sh:
  [PASS] No TODO/FIXME/HACK comments
  [PASS] No redundant null checks (defaults for optional inputs are appropriate)
  [MINOR] Line 86: double cleanup (rm -f + trap) is mild AI-style defensive duplication
  [PASS] Error messages are concise and actionable
